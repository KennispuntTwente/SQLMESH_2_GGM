# GGM-datapijplijn - Voorbeeld .env bestand
# Kopieer naar .env en vul je eigen waarden in
# 
# =============================================================================
# Pipeline
# =============================================================================
GGM_DESTINATION=postgres          # dlt: postgres, mssql, duckdb, snowflake, bigquery...
GGM_GATEWAY=local                 # SQLMesh: local, mssql, mysql, snowflake, bigquery...
GGM_SOURCE_SCHEMA=source          # Bronschema naam in Oracle

# =============================================================================
# Bron: Oracle-database van CSSD
# =============================================================================
ORACLE_HOST=localhost
ORACLE_PORT=1521
ORACLE_SERVICE_NAME=FREEPDB1
ORACLE_USER=system
ORACLE_PASSWORD=ggm_dev_123
# Oracle thick mode (optioneel):
# ORACLE_THICK_MODE=false
# Pad naar Oracle Instant Client libraries (optioneel):
# ORACLE_CLIENT_LIB_DIR=C:\oracle\instantclient_21_18

# =============================================================================
# Bestemmingen
# =============================================================================

# Vul voor de gewenste bestemming de juiste sectie in 
# (zorg dat hierboven GGM_DESTINATION overeenkomt met de gekozen bestemming)

# =============================================================================
# Bestemming: PostgreSQL (gebruikt door SQLMesh config.yaml)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=ggm_dev
POSTGRES_USER=ggm
POSTGRES_PASSWORD=ggm_dev

# =============================================================================
# Bestemming: MSSQL (gebruikt door dlt en SQLMesh config.yaml)
MSSQL_HOST=localhost
MSSQL_PORT=1433
MSSQL_DATABASE=ggm_dev
MSSQL_USER=sa
MSSQL_PASSWORD=GGM_Dev123!

# Alternatief: stel connection string direct in
# Hiermee kan je de ODBC-driver en extra parameters zoals TrustServerCertificate instellen:
# DESTINATION__MSSQL__CREDENTIALS=mssql+pyodbc://sa:GGM_Dev123!@localhost:1433/ggm_dev?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes

# =============================================================================
# Bestemming: MySQL (gebruikt door SQLMesh config.yaml)
# Let op: 'dlt' gebruikt 'sqlalchemy' voor MySQL bestemmingen

MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=ggm_dev
MYSQL_USER=root
MYSQL_PASSWORD=ggm_dev

# Alternatief: stel connection string direct in
# DESTINATION__SQLALCHEMY__CREDENTIALS=mysql://root:ggm_dev@localhost:3306/ggm_dev

# =============================================================================
# Cloud bestemmingen (vul in indien nodig)

# SNOWFLAKE_ACCOUNT=
# SNOWFLAKE_USER=
# SNOWFLAKE_PASSWORD=
# BIGQUERY_PROJECT=
